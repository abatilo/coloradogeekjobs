machine:
  node:
    version: 7.9.0

deployment:
  staging:
    branch: master
    heroku:
      appname: coloradogeekjobs

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
